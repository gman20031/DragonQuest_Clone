#include "Scene.h"

#include <cassert>

namespace BlackBoxEngine
{
    //BB_Scene::BB_Scene(std::unique_ptr<BB_Window> pWindow)
    //{
    //    assert(pWindow);
    //    m_pSceneActorManager = new ActorManager;
    //    m_pSceneActorManager->m_pOwningScene = this;
    //    m_pCollisionManager = new CollisionManager( 0 , 0 
    //        , static_cast<float>( pWindow->GetWidth() )
    //        , static_cast<float>( pWindow->GetHeight()) );
    //    m_pWindow = std::move(pWindow);
    //}

    //BB_Scene::~BB_Scene()
    //{
    //    assert(m_pSceneActorManager);
    //    delete m_pSceneActorManager;
    //}
}

